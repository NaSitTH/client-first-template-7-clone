type ProjectType = {
  allProjects: [
    {
      title: string;
      excerpt: string;
      coverImage: string;
      slug: string;
      content: string;
    }
  ];
};

export default ProjectType;
